@model List<Blog>
<div class="tech-btm">
    @foreach (var blog in Model)
    {
        <div class="blog-grids row mb-3">
            <div class="col-md-5 blog-grid-left">
                <a href="/Blog/Detail/@blog.BlogId">
                    <img src="@blog.Image" class="card-img-top img-fluid" style="width:130px;height:85px" alt="Resim bulunamadı">
                </a>
            </div>
            <div class="col-md-7 blog-grid-right">
                <h5>
                    <a href="/Blog/Detail/@blog.BlogId">@blog.Title</a>
                    @*<a href="/Blog/Detail/@blog.BlogId">@blog.Content.Substring(0, @blog.Content.Substring(0, 50).LastIndexOf(" ")) ...</a>*@
                </h5>
                <div class="sub-meta">
                    <span>
                        <i class="far fa-clock"></i> @blog.Date.ToString("dd/MM/yyyy")
                    </span>
                </div>
                <div class="sub-meta">
                    <span>
                        Kategori: @blog.Category.Name
                    </span>
                </div>
            </div>
        </div>
    }
</div>