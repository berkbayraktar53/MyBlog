@model List<About>
@{
    ViewData["Title"] = "Hakkımızda";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<section class="main-content-w3layouts-agileits">
    <div class="container">
        <h3 class="tittle">Hakkımızda</h3>
        <div class="row inner-sec">
            <div class="col-lg-8 left-blog-info-w3layouts-agileits text-left">
                @foreach (var about in Model)
                {
                    <div class="blog-grid-top">
                        <div class="blog_info_left_grid">
                            <a href="single.html">
                                <img src="@about.Image" class="img-fluid" alt="">
                            </a>
                        </div>
                        <h3>@about.Title</h3>
                        <p>@about.Content</p>
                    </div>
                }
            </div>
            <aside class="col-lg-4 agileits-w3ls-right-blog-con text-right">
                <div class="right-blog-info text-left">
                    <div class="tech-btm">
                        <img src="~/templates/blog/images/banner1.jpg" class="img-fluid" alt="">
                    </div>
                    @await Html.PartialAsync("~/Views/Newsletter/SubscribeMail.cshtml")
                    @await Html.PartialAsync("~/Views/About/SocialMediaAccount.cshtml")
                    @await Component.InvokeAsync("WriterLastBlog")
                </div>
            </aside>
        </div>
    </div>
</section>